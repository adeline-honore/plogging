name: iOS starter workflow
on: [push]

jobs:
    build:
        name: Build and Test default scheme using any available iPhone simulator
        runs-on: macos-latest

steps:
    - name: Test
        run: xcodebuild test -workspace Plogging.xcodeproj/project.xcworkspace
    -scheme TestApp -destination 'platform=iOS Simulator,
